/**
 * @typedef {import('./Card').Card} Card
 * @typedef {import('./Card').Suit} Suit
 * @typedef {import('./Card').CardValue} CardValue
 * 
 * @typedef {import('./Player').Player} Player
 * 
 * @typedef {import('./GameState').GameState} GameState
 * @typedef {import('./GameState').GameStatus} GameStatus
 * @typedef {import('./GameState').GamePhase} GamePhase
 * @typedef {import('./GameState').Dealer} Dealer
 * @typedef {import('./GameState').Winner} Winner
 * @typedef {import('./GameState').Players} Players
 * 
 * @typedef {import('./LobbyState').LobbyState} LobbyState
 * @typedef {import('./LobbyState').GameListItem} GameListItem
 */

// This file doesn't export anything directly, it just serves as a convenient
// import point for all the type definitions
module.exports = {};
